<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Metastory</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>

	<key>LSRequiresIPhoneOS</key>
	<true/>

	<!-- Programmatic UIKit app: one scene, no storyboard. -->
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneConfigurationName</key>
					<string>Default</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
				</dict>
			</array>
		</dict>
	</dict>

	<!-- Launch screen: the web app's parchment background, nothing else.
	     Anything more would flash a second, different-looking splash before
	     the page paints. -->
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>LaunchBackground</string>
	</dict>

	<!-- Keeps the Safe Inside album playing once the screen locks. Without
	     this, WebKit's media stops the moment the app is backgrounded. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>

	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<key>UIUserInterfaceStyle</key>
	<string>Light</string>

	<!-- Health. These strings are shown verbatim on the permission sheet, so
	     they say what is written and what is read, and why. -->
	<key>NSHealthUpdateUsageDescription</key>
	<string>Metastory saves the workouts you complete to Apple Health, so your training shows up alongside the rest of your activity and counts toward your rings.</string>
	<key>NSHealthShareUsageDescription</key>
	<string>Metastory reads the health data your devices and apps already record, so you are not retyping numbers you have logged elsewhere. That includes sleep, resting heart rate, HRV, respiratory rate, blood oxygen, heart rate and heart-rate recovery, steps, distance, flights, stand, move and exercise minutes, active and resting energy, workouts, walking steadiness, weight, body fat, lean mass, height, BMI, waist, VO2 max, body and wrist temperature, blood pressure, blood glucose, time in daylight, mindful minutes, water, caffeine and the nutrition you log, cycle tracking, and the symptoms you record yourself. It is used to show you your own patterns inside Metastory and to inform the guidance you ask for.</string>

	<!-- No encryption beyond standard HTTPS, so the app is exempt. Set here to
	     skip the export-compliance question on every single upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
</dict>
</plist>
